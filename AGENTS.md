## Testing

- Do not create or modify tests unless explicitly requested by the user.
- Do not add tests as part of implementing features or fixing bugs by default.
- Existing tests may be run to verify changes.
- Only create or modify tests when the task explicitly asks for tests, test coverage, or test changes.
