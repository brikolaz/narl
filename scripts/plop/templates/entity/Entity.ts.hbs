import { getEntityCreator, type Entity } from "{{coreImport}}";

const addComponents = (
  {{entityVariable}}: Entity<"{{entityType}}">,
): void => {
  void {{entityVariable}};
};

const addLoot = ({{entityVariable}}: Entity<"{{entityType}}">): void => {
  void {{entityVariable}};
};

const addEq = ({{entityVariable}}: Entity<"{{entityType}}">): void => {
  void {{entityVariable}};
};

export const {{entityName}}Entity = getEntityCreator("{{entityType}}", {
  components: addComponents,
  eq: addEq,
  loot: addLoot,
});
