import { getEntityCreator, type Entity } from "{{coreImport}}";
import { BaseMobFactory } from "{{baseFactoryImport}}";
import type { MobFactory } from "{{factoryImport}}";

const {{entityName}}Entity = getEntityCreator("{{entityType}}");
export type {{entityName}}EntityVariants = typeof {{entityName}}Entity.type;

class {{entityName}}Factory extends BaseMobFactory<{{entityName}}EntityVariants> {
  getDefault(): Entity {
    const {{entityVariable}} = {{entityName}}Entity();

    return {{entityVariable}};
  }
}

export const {{entityName}}EntityFactory =
  new {{entityName}}Factory();
